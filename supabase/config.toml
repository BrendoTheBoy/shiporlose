# Supabase local / deploy config — extend with `supabase init` output as needed.
# Stripe webhooks are verified with STRIPE_WEBHOOK_SECRET; JWT must be disabled so Stripe can POST.

[functions.stripe-webhook]
verify_jwt = false
